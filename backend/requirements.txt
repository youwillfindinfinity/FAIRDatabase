flask==3.1.0
werkzeug==3.1.3
psycopg2-binary==2.9.10
pandas==2.2.3
supabase==2.15.1
flask_cors==5.0.1
flask_limiter==3.12
python-dotenv==1.1.0
requests==2.31.0
numpy>=2.0,<2.3
dp-accounting>=0.4.4
cryptography>=42.0.0
# Plugin-owned deps live in plugins/<name>/requirements.txt:
#   pbpk         → scipy, python-libsbml
#   horizontal_fl → torch (optional; needed only by /fl/tasks/<id>/simulate)
# Installed at image build time via the Dockerfile loop and in dev via
# scripts/install-plugin-deps.sh. This file is passed as a pip --constraint
# during plugin installs so shared versions cannot drift.
pytest>=8.0
